name: Plugin Matrix Example
concurrency: 2

matrix:
  ruby:
    - "3.1"
    - "3.2"

steps:
  - name: Matrix plugin message
    uses: message
    with:
      text: Hello from matrix plugin

  - name: Matrix command
    run: ruby -e 'puts "ruby=#{ENV.fetch("MATRIX_RUBY")}"'

