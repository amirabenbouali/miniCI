source "https://rubygems.org"

gemspec

# rubocop's parallel dependency requires Ruby >= 3.3 as of 2.0.0;
# pin to the 1.x series so `bundle install` still works on Ruby 3.1/3.2 in CI.
gem "parallel", "~> 1.27"
